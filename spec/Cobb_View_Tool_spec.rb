require "spec_helper"

describe CobbViewTool do
  it "has a version number" do
    expect(CobbViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
